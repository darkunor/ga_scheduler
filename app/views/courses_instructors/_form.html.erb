<%= form_for(@courses_instructor) do |f| %>
  <% if @courses_instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courses_instructor.errors.count, "error") %> prohibited this courses_instructor from being saved:</h2>

      <ul>
      <% @courses_instructor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :instructor_id %><br />
    <%= f.number_field :instructor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
